import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  orderText: {
    marginTop: 40,
    fontSize: 19,
    fontWeight: "600",
    textAlign: "center",
  },
  backToHome: { fontSize: 17, fontWeight: "600", color: "black" },
});
