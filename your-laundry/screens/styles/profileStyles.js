import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  profileView: { flex: 1, justifyContent: "center", alignItems: "center" },
  profilePress: { marginVertical: 10 },
});
